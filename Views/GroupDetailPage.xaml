<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:NeatSplit.Views"
             x:Class="NeatSplit.Views.GroupDetailPage"
             Title="Group Details">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Group Info -->
            <Frame Grid.Row="0" Margin="10" Padding="15" CornerRadius="12" HasShadow="True" BackgroundColor="White">
                <StackLayout>
                    <Label x:Name="GroupNameLabel" FontSize="24" FontAttributes="Bold" />
                    <Label x:Name="GroupDescriptionLabel" FontSize="16" TextColor="Gray" />
                    <Label x:Name="MemberCountLabel" FontSize="14" TextColor="DarkGreen" />
                </StackLayout>
            </Frame>

            <!-- Tab View -->
            <TabView Grid.Row="1" Margin="10,0,10,10">
                <TabViewItem Title="Members">
                    <views:MembersTab x:Name="MembersTab" />
                </TabViewItem>
                <TabViewItem Title="Expenses">
                    <views:ExpensesTab x:Name="ExpensesTab" />
                </TabViewItem>
                <TabViewItem Title="Balances">
                    <views:BalancesTab x:Name="BalancesTab" />
                </TabViewItem>
            </TabView>
        </Grid>
    </ContentPage.Content>
</ContentPage> 