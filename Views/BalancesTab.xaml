<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NeatSplit.Views.BalancesTab"
             Title="Balances">
    <ContentPage.Content>
        <Grid>
            <CollectionView x:Name="BalancesCollectionView"
                            ItemsSource="{Binding Balances}"
                            Margin="10,10,10,80">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,5" Padding="10" CornerRadius="12" HasShadow="True" BackgroundColor="White">
                            <Frame.Triggers>
                                <EventTrigger Event="Appearing">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Frame.Triggers>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackLayout Grid.Column="0">
                                    <Label Text="{Binding MemberName}" FontAttributes="Bold" FontSize="16" />
                                    <Label Text="{Binding Amount, StringFormat='Balance: ${0:F2}'}" FontSize="14" TextColor="Gray" />
                                </StackLayout>
                                <Label Grid.Column="1" 
                                       Text="{Binding Amount, StringFormat='${0:F2}'}" 
                                       FontSize="18" 
                                       FontAttributes="Bold"
                                       VerticalOptions="Center"
                                       TextColor="{Binding Amount, Converter={StaticResource BalanceColorConverter}}" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </ContentPage.Content>
</ContentPage> 